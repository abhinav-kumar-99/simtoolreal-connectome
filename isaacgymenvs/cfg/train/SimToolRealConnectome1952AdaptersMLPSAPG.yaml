defaults:
  - SimToolRealConnectome1952AdaptersSAPG
  - _self_

params:
  network:
    connectome:
      interface_projections:
        architecture: mlp
        hidden_size: 256
        activation: elu
  config:
    name: ${resolve_default:SimToolRealConnectome1952AdaptersMLPSAPG,${....experiment}}
