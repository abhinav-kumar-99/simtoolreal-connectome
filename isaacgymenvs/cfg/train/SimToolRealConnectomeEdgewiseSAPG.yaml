defaults:
  - SimToolRealConnectomeSAPG
  - _self_

params:
  network:
    connectome:
      adaptation:
        weight_mode: edgewise
        learn_dynamics: false
      spectral_monitoring:
        enabled: true
        interval: 100
        top_k: 8
  config:
    name: ${resolve_default:SimToolRealConnectomeEdgewiseSAPG,${....experiment}}
