defaults:
  - SimToolRealConnectomeSAPG
  - _self_

params:
  network:
    connectome:
      artifact_path: data/connectomes/processed/malecns_4310/random.npz
      graph_variant: random

  config:
    name: ${resolve_default:SimToolRealConnectomeRandomSAPG,${....experiment}}
