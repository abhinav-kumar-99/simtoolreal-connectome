defaults:
  - SimToolRealConnectomeSAPG
  - _self_

params:
  network:
    connectome:
      adaptation:
        weight_mode: neuron_gains
        learn_dynamics: false
  config:
    name: ${resolve_default:SimToolRealConnectomeGainsSAPG,${....experiment}}

