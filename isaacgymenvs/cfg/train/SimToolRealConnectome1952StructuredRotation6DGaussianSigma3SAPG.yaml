defaults:
  - SimToolRealConnectome1952StructuredRotation6DSAPG
  - _self_

params:
  network:
    space:
      continuous:
        distribution: gaussian
        fixed_sigma: coef_cond
        max_sigma: 3.0
  config:
    name: ${resolve_default:SimToolRealConnectome1952StructuredRotation6DGaussianSigma3SAPG,${....experiment}}
